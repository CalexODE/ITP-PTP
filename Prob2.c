#include <stdio.h>

int main()
{
	/*decrarão da matriz e seus valores*/
	int i=0,j=0;
	do{
		do{
			printf("%d ", matriz[i][j]);
			if (i == 0 || j == colunas)
			{
				cont++;
				break;
			}
			i--;
			j++;

		}while(true);
		i == cont;
		j == 0
	}while(i != linhas || j != colunas);
	return 0;
}